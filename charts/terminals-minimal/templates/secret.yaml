apiVersion: v1
kind: Secret
metadata:
  name: openwebui-terminals-api-key
  namespace: open-webui
  labels:
    app.kubernetes.io/name: openwebui-terminals
    app.kubernetes.io/component: orchestrator
    app.kubernetes.io/part-of: open-terminal
type: Opaque
stringData:
  # Shared bearer key between Open WebUI and the orchestrator.
  # CHANGE THIS to your own value, e.g. `openssl rand -hex 24`, and use the
  # same value in Open WebUI's TERMINAL_SERVER_CONNECTIONS.
  api-key: "81KW2Gl4TsjdiB0lw8VbGRJtVkyfFVmp0QmvbY73"
