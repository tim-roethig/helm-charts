apiVersion: v1
kind: Service
metadata:
  name: {{ include "terminals-minimal.fullname" . }}
  labels:
    {{- include "terminals-minimal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "terminals-minimal.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
